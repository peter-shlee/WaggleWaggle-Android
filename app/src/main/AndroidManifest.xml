<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.somasoma.wagglewaggle">

    <application
        android:name=".core.SpeakWorldApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpeakWorld"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".presentation.auth.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.follower_following.FollowerFollowingActivity"
            android:theme="@style/noAnimeTheme" />
        <activity
            android:name=".presentation.main.MainActivity"
            android:theme="@style/noAnimeTheme" />
        <activity android:name=".presentation.main.create_world.CreateWorldActivity" />
        <activity android:name=".presentation.profile.ProfileActivity" />
        <activity android:name=".presentation.setting.EditProfileActivity" />
        <activity android:name=".presentation.auth.sign_up.SignUpActivity" />
        <activity android:name=".presentation.setting.SettingActivity" />
        <activity android:name=".presentation.auth.sign_in_and_sign_up.SignInAndSignUpActivity" />
    </application>

</manifest>